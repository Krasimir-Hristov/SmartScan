# SmartScan

AI-powered document and receipt scanning application.

## Project Structure

- `backend/` - FastAPI backend with LangChain & LangGraph
- `frontend/` - Next.js (React 19, Tailwind CSS v4, TypeScript) frontend
